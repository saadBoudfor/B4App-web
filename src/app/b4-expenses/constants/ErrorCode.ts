export enum ErrorCode {
  INCOME_NOT_FOUND = 'income not found',
  ID_ALREADY_EXIST= 'income with the same id is already saved in db',
  INVALID_INCOME = 'income is invalid',
  ID_NOT_FOUND = 'id is required',
  NAVIGATION_MODEL_EMPTY = 'Navigation component was called with empty model'
}
